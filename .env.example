# Required by Docker Compose. Use a unique, high-entropy value outside source control.
POSTGRES_PASSWORD=replace-with-a-unique-password
# Optional host port for the application.
HUM_PORT=3000
# Optional override for a prebuilt image, e.g. ghcr.io/talexdreamsoul/hum:dev.
HUM_IMAGE=hum:local
